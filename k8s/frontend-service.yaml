apiVersion: v1
kind: Service
metadata:
  name: trabawho-frontend
  namespace: trabawho
  labels:
    app: trabawho
    component: frontend
spec:
  type: NodePort
  selector:
    app: trabawho
    component: frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
